import React from 'react'

const HideLoginButonn = ({onButtonClick}) => {
  return (
    <div>
        <button className='button' onClick={onButtonClick}>Back to menu</button>
    </div>
  )
}

export default HideLoginButonn;